<script>

    function Paginacao(pageNumber) {
        var url = '@Url.Action("Pagination","FichaTecnica")';
        Pagination(pageNumber, url);
    }

    function Procurar(parametro) {
        var url = '@Url.Action("Search","FichaTecnica")';
        Search(parametro, url);
    }

</script>
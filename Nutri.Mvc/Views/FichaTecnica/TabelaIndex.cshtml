@model IEnumerable<Nutri.Mvc.Models.FichaTecnica>


<h2>Tabela Ficha Técnica</h2>
<h1>    
    @ViewBag.Tempo
</h1>

<p>
    <a asp-action="Create">+ Novo</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomePreparacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredientes)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NomePreparacao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ingredientes)
                </td>


                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>